{
  "promptTitle": "Final Onboarding Blueprint for InStyle on a Custom Next.js Platform",
  "persona": "You are an expert full-stack developer and UI/UX designer specializing in creating production-ready, multi-tenant SaaS applications using Next.js, Tailwind CSS, and Supabase. You write clean, efficient, and visually appealing code.",
  "coreObjective": "Your mission is to generate the **complete, multi-file Next.js application code** required to finally onboard our first client, 'InStyle Hair Boutique', onto our **custom-built platform**. This is the definitive implementation, moving away from any temporary or reseller solutions. The user should be able to sign up, create their salon, manage services, and have a live, public booking page on a subdomain. The final output must be a single, parseable JSON object where keys are file paths and values are the complete code for those files.",
  "technicalContext": {
    "stack": "Next.js (with Pages Router), React, Tailwind CSS, Supabase.",
    "database": "The Supabase database is already set up with our `v3.1` schema. It includes tables for `salons`, `profiles`, `services`, `appointments`, etc., with all necessary RLS policies. You do not need to generate any SQL.",
    "authentication": "Authentication will be handled by Supabase Auth, using the `@supabase/auth-helpers-react` and `@supabase/auth-helpers-nextjs` libraries.",
    "environment": "This code will be run in a Replit environment. All necessary environment variables (like `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`) will be available.",
    "constraint": "You cannot use any file-system-aware agent commands. You must generate all files and their full contents within the final JSON object."
  },
  "keyUserStories": [
    "As a new salon owner, I can sign up for an account.",
    "After signing up, I can create my salon profile, including a unique subdomain.",
    "Once my salon is created, I can access a simple dashboard to add and manage my services (name, price, duration).",
    "As a client, I can visit my salon's unique subdomain (e.g., `instyle.repl.co`).",
    "On the salon's public page, I can see their list of services and book an appointment.",
    "As the salon owner, new bookings should appear in my database."
  ],
  "instructions": {
    "outputFormat": "Generate a single, valid JSON object. The keys of this object must be the full file paths (e.g., `pages/signup.jsx`). The values must be a string containing the complete, production-ready code for that file.",
    "uiRequirements": "Use **Tailwind CSS** for all styling. The UI must be clean, modern, professional, and fully responsive. The public-facing booking page for InStyle should feel premium and on-brand. The salon owner dashboard should be functional and intuitive.",
    "codeQuality": "Code must be clean, well-commented where necessary, and follow best practices for React and Next.js development. Implement basic error handling and user feedback (e.g., loading states, success/error messages)."
  },
  "deliverable": {
    "description": "The final JSON object containing the complete file structure and code for the InStyle Onboarding MVP.",
    "files": {
      "package.json": "---\n{\n  \"name\": \"appointmentbookings-saas\",\n  \"version\": \"1.0.0\",\n  \"private\": true,\n  \"scripts\": {\n    \"dev\": \"next dev\",\n    \"build\": \"next build\",\n    \"start\": \"next start\",\n    \"lint\": \"next lint\"\n  },\n  \"dependencies\": {\n    \"@supabase/auth-helpers-nextjs\": \"^0.8.7\",\n    \"@supabase/auth-helpers-react\": \"^0.4.2\",\n    \"@supabase/supabase-js\": \"^2.39.0\",\n    \"date-fns\": \"^2.30.0\",\n    \"next\": \"14.0.4\",\n    \"react\": \"^18\",\n    \"react-dom\": \"^18\",\n    \"react-hot-toast\": \"^2.4.1\"\n  },\n  \"devDependencies\": {\n    \"autoprefixer\": \"^10.4.16\",\n    \"eslint\": \"^8\",\n    \"eslint-config-next\": \"14.0.4\",\n    \"postcss\": \"^8.4.32\",\n    \"tailwindcss\": \"^3.4.0\"\n  }\n}\n---",
      "tailwind.config.js": "---\n/** @type {import('tailwindcss').Config} */\nmodule.exports = {\n  content: [\n    './pages/**/*.{js,ts,jsx,tsx,mdx}',\n    './components/**/*.{js,ts,jsx,tsx,mdx}',\n  ],\n  theme: {\n    extend: {\n      colors: {\n        primary: '#6d28d9',\n        'primary-dark': '#5b21b6',\n      }\n    },\n  },\n  plugins: [],\n}\n---",
      "postcss.config.js": "---\nmodule.exports = {\n  plugins: {\n    tailwindcss: {},\n    autoprefixer: {},\n  },\n}\n---",
      "styles/globals.css": "---\n@tailwind base;\n@tailwind components;\n@tailwind utilities;\n---",
      "lib/supabaseClient.js": "---\nimport { createPagesBrowserClient } from '@supabase/auth-helpers-nextjs'\n\nexport const supabase = createPagesBrowserClient()\n---",
      "components/Layout.jsx": "---\nimport Link from 'next/link';\nimport { useUser, useSupabaseClient } from '@supabase/auth-helpers-react';\nimport { useRouter } from 'next/router';\n\nexport default function Layout({ children }) {\n  const user = useUser();\n  const supabaseClient = useSupabaseClient();\n  const router = useRouter();\n\n  async function handleLogout() {\n    await supabaseClient.auth.signOut();\n    router.push('/');\n  }\n\n  return (\n    <div className=\"min-h-screen bg-gray-100\">\n      <header className=\"bg-white shadow-sm\">\n        <nav className=\"container mx-auto px-6 py-3\">\n          <div className=\"flex justify-between items-center\">\n            <Link href=\"/\" className=\"text-xl font-bold text-primary\">\n              AppointmentBookings.co.za\n            </Link>\n            <div>\n              {user ? (\n                <div className=\"flex items-center space-x-4\">\n                  <Link href=\"/dashboard/manage\" className=\"text-gray-700 hover:text-primary\">\n                    Dashboard\n                  </Link>\n                  <button onClick={handleLogout} className=\"bg-gray-200 text-gray-700 px-4 py-2 rounded-md text-sm font-medium hover:bg-gray-300\">\n                    Logout\n                  </button>\n                </div>\n              ) : (\n                <div className=\"space-x-4\">\n                  <Link href=\"/login\" className=\"text-gray-700 hover:text-primary\">\n                    Login\n                  </Link>\n                  <Link href=\"/signup\" className=\"bg-primary text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-primary-dark\">\n                    Start Free Trial\n                  </Link>\n                </div>\n              )}\n            </div>\n          </div>\n        </nav>\n      </header>\n      <main>{children}</main>\n    </div>\n  );\n}\n---",
      "pages/_app.js": "---\nimport '@/styles/globals.css';\nimport { createPagesBrowserClient } from '@supabase/auth-helpers-nextjs';\nimport { SessionContextProvider } from '@supabase/auth-helpers-react';\nimport { useState } from 'react';\nimport { Toaster } from 'react-hot-toast';\nimport Layout from '../components/Layout';\n\nexport default function App({ Component, pageProps }) {\n  const [supabaseClient] = useState(() => createPagesBrowserClient());\n\n  return (\n    <SessionContextProvider\n      supabaseClient={supabaseClient}\n      initialSession={pageProps.initialSession}\n    >\n      <Layout>\n        <Component {...pageProps} />\n        <Toaster />\n      </Layout>\n    </SessionContextProvider>\n  );\n}\n---",
      "pages/index.jsx": "---\nimport Head from 'next/head';\n\nexport default function HomePage() {\n  return (\n    <>\n      <Head>\n        <title>AppointmentBookings.co.za - AI-Powered Salon Management</title>\n      </Head>\n      <div className=\"container mx-auto text-center py-20 px-6\">\n        <h1 className=\"text-5xl font-bold text-gray-800 mb-4\">The All-in-One Platform to Manage & Grow Your Salon</h1>\n        <p className=\"text-xl text-gray-600 max-w-3xl mx-auto\">From intelligent AI-powered bookings and seamless e-commerce to automated client reminders, this is the last platform you'll ever need.</p>\n      </div>\n    </>\n  );\n}\n---",
      "pages/signup.jsx": "---\nimport { useState } from 'react';\nimport { supabase } from '@/lib/supabaseClient';\nimport { useRouter } from 'next/router';\nimport toast from 'react-hot-toast';\n\nexport default function SignUp() {\n  const [email, setEmail] = useState('');\n  const [password, setPassword] = useState('');\n  const [loading, setLoading] = useState(false);\n  const router = useRouter();\n\n  const handleSignUp = async (e) => {\n    e.preventDefault();\n    setLoading(true);\n    const { error } = await supabase.auth.signUp({ email, password });\n    if (error) {\n      toast.error(error.message);\n    } else {\n      toast.success('Check your email for the confirmation link!');\n      router.push('/dashboard/create-salon'); // Redirect to create salon page\n    }\n    setLoading(false);\n  };\n\n  return (\n    <div className=\"flex justify-center items-center mt-20\">\n      <div className=\"w-full max-w-md p-8 space-y-6 bg-white rounded-lg shadow-md\">\n        <h1 className=\"text-2xl font-bold text-center\">Create Your Account</h1>\n        <form onSubmit={handleSignUp} className=\"space-y-6\">\n          <div>\n            <label>Email</label>\n            <input type=\"email\" value={email} onChange={(e) => setEmail(e.target.value)} required className=\"w-full px-4 py-2 mt-2 border rounded-md\" />\n          </div>\n          <div>\n            <label>Password</label>\n            <input type=\"password\" value={password} onChange={(e) => setPassword(e.target.value)} required className=\"w-full px-4 py-2 mt-2 border rounded-md\" />\n          </div>\n          <button type=\"submit\" disabled={loading} className=\"w-full px-4 py-2 font-bold text-white bg-primary rounded-md hover:bg-primary-dark\">\n            {loading ? 'Signing up...' : 'Sign Up'}\n          </button>\n        </form>\n      </div>\n    </div>\n  );\n}\n---",
      "pages/login.jsx": "---\nimport { useState } from 'react';\nimport { supabase } from '@/lib/supabaseClient';\nimport { useRouter } from 'next/router';\nimport toast from 'react-hot-toast';\n\nexport default function Login() {\n  const [email, setEmail] = useState('');\n  const [password, setPassword] = useState('');\n  const [loading, setLoading] = useState(false);\n  const router = useRouter();\n\n  const handleLogin = async (e) => {\n    e.preventDefault();\n    setLoading(true);\n    const { error } = await supabase.auth.signInWithPassword({ email, password });\n    if (error) {\n      toast.error(error.message);\n    } else {\n      router.push('/dashboard/manage');\n    }\n    setLoading(false);\n  };\n\n  return (\n    <div className=\"flex justify-center items-center mt-20\">\n      <div className=\"w-full max-w-md p-8 space-y-6 bg-white rounded-lg shadow-md\">\n        <h1 className=\"text-2xl font-bold text-center\">Login to Your Account</h1>\n        <form onSubmit={handleLogin} className=\"space-y-6\">\n          <div>\n            <label>Email</label>\n            <input type=\"email\" value={email} onChange={(e) => setEmail(e.target.value)} required className=\"w-full px-4 py-2 mt-2 border rounded-md\" />\n          </div>\n          <div>\n            <label>Password</label>\n            <input type=\"password\" value={password} onChange={(e) => setPassword(e.target.value)} required className=\"w-full px-4 py-2 mt-2 border rounded-md\" />\n          </div>\n          <button type=\"submit\" disabled={loading} className=\"w-full px-4 py-2 font-bold text-white bg-primary rounded-md hover:bg-primary-dark\">\n            {loading ? 'Logging in...' : 'Login'}\n          </button>\n        </form>\n      </div>\n    </div>\n  );\n}\n---",
      "pages/dashboard/create-salon.jsx": "---\nimport { useState } from 'react';\nimport { useUser, useSupabaseClient } from '@supabase/auth-helpers-react';\nimport { useRouter } from 'next/router';\nimport toast from 'react-hot-toast';\n\nexport default function CreateSalon() {\n  const user = useUser();\n  const supabase = useSupabaseClient();\n  const router = useRouter();\n  const [subdomain, setSubdomain] = useState('');\n  const [salonName, setSalonName] = useState('');\n  const [loading, setLoading] = useState(false);\n\n  const handleCreateSalon = async (e) => {\n    e.preventDefault();\n    if (!user) {\n      toast.error('You must be logged in to create a salon.');\n      return;\n    }\n    setLoading(true);\n\n    // Check if subdomain is available\n    const { count } = await supabase.from('salons').select('*', { count: 'exact' }).eq('subdomain', subdomain);\n    if (count > 0) {\n      toast.error('This subdomain is already taken. Please choose another.');\n      setLoading(false);\n      return;\n    }\n\n    const { error } = await supabase.from('salons').insert({\n      name: salonName,\n      subdomain,\n      owner_id: user.id,\n      plan: 'trial',\n      trial_ends_at: new Date(Date.now() + 14 * 24 * 60 * 60 * 1000).toISOString()\n    });\n\n    if (error) {\n      toast.error(error.message);\n    } else {\n      toast.success('Salon created successfully!');\n      router.push('/dashboard/manage');\n    }\n    setLoading(false);\n  };\n\n  return (\n    <div className=\"container mx-auto mt-10 max-w-2xl\">\n      <div className=\"p-8 bg-white rounded-lg shadow-md\">\n        <h1 className=\"text-2xl font-bold mb-6\">Set Up Your Salon</h1>\n        <form onSubmit={handleCreateSalon} className=\"space-y-4\">\n          <div>\n            <label>Salon Name</label>\n            <input type=\"text\" value={salonName} onChange={(e) => setSalonName(e.target.value)} required className=\"w-full px-4 py-2 mt-2 border rounded-md\" />\n          </div>\n          <div>\n            <label>Choose Your Subdomain</label>\n            <div className=\"flex items-center mt-2\">\n              <input type=\"text\" value={subdomain} onChange={(e) => setSubdomain(e.target.value.toLowerCase().replace(/[^a-z0-9-]/g, ''))} required className=\"w-full px-4 py-2 border rounded-l-md\" />\n              <span className=\"px-4 py-2 bg-gray-200 border border-l-0 rounded-r-md\">.appointmentbookings.co.za</span>\n            </div>\n            <p className='text-sm text-gray-500 mt-1'>Your public booking page will be here. Use only letters, numbers, and hyphens.</p>\n          </div>\n          <button type=\"submit\" disabled={loading} className=\"w-full px-4 py-2 font-bold text-white bg-primary rounded-md hover:bg-primary-dark\">\n            {loading ? 'Creating...' : 'Create Salon'}\n          </button>\n        </form>\n      </div>\n    </div>\n  );\n}\n---",
      "pages/dashboard/manage.jsx": "---\nimport { useState, useEffect } from 'react';\nimport { useUser, useSupabaseClient } from '@supabase/auth-helpers-react';\nimport toast from 'react-hot-toast';\n\nexport default function ManageDashboard() {\n  const user = useUser();\n  const supabase = useSupabaseClient();\n  const [salon, setSalon] = useState(null);\n  const [services, setServices] = useState([]);\n  const [newService, setNewService] = useState({ name: '', duration: 30, price: '' });\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    const fetchData = async () => {\n      if (user) {\n        // Fetch salon owned by the user\n        const { data: salonData, error: salonError } = await supabase.from('salons').select('*').eq('owner_id', user.id).single();\n        if (salonData) {\n          setSalon(salonData);\n          // Fetch services for that salon\n          const { data: servicesData, error: servicesError } = await supabase.from('services').select('*').eq('salon_id', salonData.id);\n          if (servicesData) setServices(servicesData);\n        } else if (!salonData && !salonError) {\n          // If no salon found, maybe they need to create one.\n          // router.push('/dashboard/create-salon');\n        }\n        setLoading(false);\n      }\n    };\n    fetchData();\n  }, [user]);\n\n  const handleAddService = async (e) => {\n    e.preventDefault();\n    const { error } = await supabase.from('services').insert({\n      salon_id: salon.id,\n      name: newService.name,\n      duration_minutes: parseInt(newService.duration),\n      price: parseFloat(newService.price)\n    }).select();\n    if (error) {\n      toast.error(error.message);\n    } else {\n      toast.success('Service added!');\n      setServices([...services, { ...newService, salon_id: salon.id, id: Math.random() }]); // Basic update\n      setNewService({ name: '', duration: 30, price: '' });\n    }\n  };\n\n  if (loading) return <div className=\"container mx-auto text-center p-10\">Loading dashboard...</div>;\n  if (!salon) return <div className=\"container mx-auto text-center p-10\">No salon found. Please create one.</div>;\n\n  return (\n    <div className=\"container mx-auto p-6\">\n      <h1 className=\"text-3xl font-bold mb-2\">Manage {salon.name}</h1>\n      <p className='text-gray-600 mb-8'>Your public booking page: <a href={`http://${salon.subdomain}.localhost:3000`} target='_blank' className='text-primary font-medium'>{salon.subdomain}.localhost:3000</a></p>\n\n      <div className=\"bg-white p-6 rounded-lg shadow-md\">\n        <h2 className=\"text-xl font-bold mb-4\">Manage Your Services</h2>\n        <form onSubmit={handleAddService} className=\"grid grid-cols-1 md:grid-cols-4 gap-4 mb-8 items-end\">\n          <div className='md:col-span-2'>\n            <label>Service Name</label>\n            <input type='text' value={newService.name} onChange={e => setNewService({...newService, name: e.target.value})} className='w-full border rounded-md p-2 mt-1' required />\n          </div>\n          <div>\n            <label>Duration (min)</label>\n            <input type='number' value={newService.duration} onChange={e => setNewService({...newService, duration: e.target.value})} className='w-full border rounded-md p-2 mt-1' required />\n          </div>\n          <div>\n            <label>Price (R)</label>\n            <input type='number' step='0.01' value={newService.price} onChange={e => setNewService({...newService, price: e.target.value})} className='w-full border rounded-md p-2 mt-1' required />\n          </div>\n          <button type='submit' className='bg-primary text-white p-2 rounded-md font-medium h-fit'>Add Service</button>\n        </form>\n\n        <h3 className=\"text-lg font-semibold mb-4\">Current Services</h3>\n        <div className=\"space-y-2\">\n          {services.map(s => (\n            <div key={s.id} className='flex justify-between items-center p-3 bg-gray-50 rounded-md'>\n              <span>{s.name}</span>\n              <span>{s.duration_minutes} min</span>\n              <span>R{s.price}</span>\n            </div>\n          ))}\n        </div>\n      </div>\n    </div>\n  );\n}\n---",
      "pages/[subdomain]/index.jsx": "---\nimport { createServerSupabaseClient } from '@supabase/auth-helpers-nextjs';\nimport { useState } from 'react';\nimport toast from 'react-hot-toast';\nimport Head from 'next/head';\n\nexport default function SalonBookingPage({ salon, services, error }) {\n  const [selectedService, setSelectedService] = useState(null);\n  const [clientDetails, setClientDetails] = useState({ name: '', phone: '' });\n\n  const handleBooking = async () => {\n    if (!selectedService || !clientDetails.name || !clientDetails.phone) {\n      toast.error('Please select a service and provide your details.');\n      return;\n    }\n    toast.success(`Booking request sent for ${selectedService.name}! We will contact you to confirm the time.`);\n    // In a full implementation, this would call an API to create the appointment.\n  };\n\n  if (error) return <div className=\"text-center p-20\"><h1>Error</h1><p>{error}</p></div>;\n  if (!salon) return <div className=\"text-center p-20\"><h1>Salon Not Found</h1><p>The salon you are looking for does not exist.</p></div>;\n\n  return (\n    <>\n      <Head>\n        <title>Book an Appointment at {salon.name}</title>\n      </Head>\n      <div className=\"container mx-auto p-6 max-w-3xl\">\n        <div className=\"text-center mb-10\">\n          <h1 className=\"text-4xl font-bold\">{salon.name}</h1>\n          <p className=\"text-lg text-gray-600 mt-2\">Book your appointment with us</p>\n        </div>\n\n        <div className=\"bg-white p-8 rounded-lg shadow-lg\">\n          <h2 className=\"text-2xl font-semibold mb-6\">1. Select a Service</h2>\n          <div className=\"grid grid-cols-1 md:grid-cols-2 gap-4\">\n            {services.map(service => (\n              <div \n                key={service.id} \n                onClick={() => setSelectedService(service)}\n                className={`p-4 border rounded-lg cursor-pointer transition-all ${selectedService?.id === service.id ? 'bg-primary text-white border-primary shadow-md' : 'hover:bg-gray-50'}`}>\n                <h3 className=\"font-bold\">{service.name}</h3>\n                <p>{service.duration_minutes} minutes - R{service.price}</p>\n              </div>\n            ))}\n          </div>\n\n          {selectedService && (\n            <div className=\"mt-10\">\n              <h2 className=\"text-2xl font-semibold mb-6\">2. Your Details</h2>\n              <div className=\"grid grid-cols-1 md:grid-cols-2 gap-4\">\n                <input type='text' placeholder='Your Name' value={clientDetails.name} onChange={e => setClientDetails({...clientDetails, name: e.target.value})} className='w-full border rounded-md p-3' />\n                <input type='text' placeholder='Your Phone Number' value={clientDetails.phone} onChange={e => setClientDetails({...clientDetails, phone: e.target.value})} className='w-full border rounded-md p-3' />\n              </div>\n            </div>\n          )}\n\n          <button \n            onClick={handleBooking} \n            disabled={!selectedService}\n            className=\"w-full mt-10 p-4 text-lg font-bold text-white bg-primary rounded-md hover:bg-primary-dark disabled:opacity-50 disabled:cursor-not-allowed\">\n            Request Booking\n          </button>\n        </div>\n      </div>\n    </>\n  );\n}\n\nexport const getServerSideProps = async (ctx) => {\n  const { subdomain } = ctx.params;\n  const supabase = createServerSupabaseClient(ctx);\n\n  try {\n    // Fetch salon by subdomain\n    const { data: salon, error: salonError } = await supabase\n      .from('salons')\n      .select('id, name')\n      .eq('subdomain', subdomain)\n      .single();\n\n    if (salonError || !salon) {\n      return { props: { error: 'Salon not found.' } };\n    }\n\n    // Fetch services for that salon\n    const { data: services, error: servicesError } = await supabase\n      .from('services')\n      .select('*')\n      .eq('salon_id', salon.id);\n\n    if (servicesError) {\n      return { props: { error: 'Could not fetch services.' } };\n    }\n\n    return { props: { salon, services } };\n  } catch (e) {\n    return { props: { error: 'An unexpected error occurred.' } };\n  }\n};\n---"
    }
  }
}