---
globs:
  - "**/*.{js,jsx,ts,tsx}"
description: When rendering lists in React, always use a unique and stable ID as
  the key prop. If no such ID exists, consider generating one or restructuring
  the data.
---

When rendering lists in React, always use a unique and stable ID as the key prop. If no such ID exists, consider generating one or restructuring the data.