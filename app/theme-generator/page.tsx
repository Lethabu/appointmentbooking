import ThemeGenerator from "@/components/ThemeGenerator";

export default function ThemeGeneratorPage() {
  return (
    <main>
      <ThemeGenerator authToken={"YOUR_AUTH_TOKEN"} />
    </main>
  );
}
